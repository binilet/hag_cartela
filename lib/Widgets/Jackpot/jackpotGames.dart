import 'package:flutter/material.dart';

class JackpotGames extends StatelessWidget{
  @override
  Widget build(BuildContext context){
    return Center(
      child: Text(
        'Games Tab content',
        style: TextStyle(fontSize: 28),
      ),
    );
  }
}